/**
 * Editor UI feature flags. Toggle visibility of certain editor controls without
 * removing underlying logic (e.g. so they can be reintroduced later).
 */
/** When false, only the "Buildings" header and "+ Add" button are hidden. Batch placement UI is always shown. */
export const SHOW_SINGLE_BUILDING_ADD_UI = false;
